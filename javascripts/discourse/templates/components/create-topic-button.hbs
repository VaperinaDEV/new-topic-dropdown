{{#if this.canCreateTopic}}
  <DButton @class={{this.btnClass}} @id="create-topic" @action={{this.action}} @icon="pen" @disabled={{this.disabled}} @label={{this.label}} />
  {{new-topic-dropdown category=category}}
{{/if}}
